import { keyframes } from "@vanilla-extract/css";

export const loadingAnimation = keyframes({
  "0%": {
    transform: "translateY(0)",
  },
  "50%": { transform: "translateY(-9px)" },
  "100%": {
    transform: "translateY(0)",
  },
});

export const clickAnimation = keyframes({
  "0%": { transform: "scale(1)" },
  "50%": { transform: "scale(0.95)" },
  "100%": { transform: "scale(1)" },
});

export const titleAnimation = keyframes({
  "0%": { top: "224px" },
  "25%": { top: "218px" },
  "50%": { top: "212px" },
  "75%": { top: "218px" },
  "100%": { top: "224px" },
});

export const rotateSixAnimation = keyframes({
  "0%": { transform: "rotate(0)" },
  "50%": { transform: "rotate(-6deg)" },
  "100%": { transform: "rotate(0)" },
});

export const rotateTenAnimation = keyframes({
  "0%": { transform: "rotate(0)" },
  "50%": { transform: "rotate(10deg)" },
  "100%": { transform: "rotate(0)" },
});

export const textBalloonAnimation = keyframes({
  "0%": { transform: "translateZ(0) translate(0, 0)" },
  "50%": { transform: "translateZ(0) translateY(-11px)" },
  "100%": { transform: "translateZ(0) translate(0, 0)" },
});

export const micAnimation1 = keyframes({
  "0%": { transform: "translateZ(0) translate(0, 0)" },
  "50%": { transform: "ranslateZ(0) translate(-4px, 4px)" },
  "100%": { transform: "translateZ(0) translate(0, 0)" },
});

export const micAnimation2 = keyframes({
  "0%": { transform: "translateZ(0) translate(0, 0)" },
  "50%": { transform: "translateZ(0) translate(7px, -3px)" },
  "100%": { transform: "translateZ(0) translate(0, 0)" },
});

export const ticketAnimation = keyframes({
  "0%": { transform: "translateZ(0) translate(0, 0)" },
  "50%": { transform: "translateZ(0) rotate(-7deg)" },
  "100%": { transform: "translateZ(0) translate(0, 0)" },
});

export const effect1Animation = keyframes({
  "0%": { transform: "translateZ(0) translate(0, 0)" },
  "50%": {
    transform: "translateZ(0) translateZ(0) rotate(-15deg)",
    left: "336px",
    top: "264px",
  },
  "100%": { transform: "translateZ(0) translate(0, 0)" },
});

export const effect2Animation = keyframes({
  "0%": { transform: "translateZ(0) translate(0, 0)" },
  "50%": {
    transform: "translateZ(0) translateZ(0) rotate(-10deg)",
    left: "287px",
    top: "261px",
  },
  "100%": { transform: "translateZ(0) translate(0, 0)" },
});

export const effect3Animation = keyframes({
  "0%": { transform: "translateZ(0) translate(0, 0)" },
  "50%": {
    transform: "translateZ(0) translateZ(0) rotate(-10deg)",
    left: "362px",
    top: "295px",
  },
  "100%": { transform: "translateZ(0) translate(0, 0)" },
});

export const sandGlassAnimation = keyframes({
  "0%": { transform: "translateZ(0) rotate(3deg)" },
  "50%": { transform: "translateZ(0) rotate(-3deg)" },
  "100%": { transform: "translateZ(0) rotate(3deg)" },
});

export const textBalloon2Animation = keyframes({
  "0%": { transform: "translateZ(0) translateY(0)" },
  "50%": { transform: "translateZ(0) translateY(-11px)" },
  "100%": { transform: "translateZ(0) translateY(0)" },
});

export const like1Animation = keyframes({
  "0%": { transform: "translateZ(0) translateY(0)" },
  "50%": { transform: "translateZ(0) translateY(4px);" },
  "100%": { transform: "translateZ(0) translateY(0)" },
});

export const like2Animation = keyframes({
  "0%": { transform: "translateZ(0) translateY(0)" },
  "50%": { transform: "translateZ(0) translateY(20px)" },
  "100%": { transform: "translateZ(0) translateY(0)" },
});

export const like3Animation = keyframes({
  "0%": { transform: "translateZ(0) translateY(0)" },
  "50%": { transform: "translateZ(0) translateY(5px)" },
  "100%": { transform: "translateZ(0) translateY(0)" },
});

export const miniatureAnimation = keyframes({
  "0%": { transform: "translateZ(0) rotate(8deg)" },
  "50%": { transform: "translateZ(0) rotate(-3deg)" },
  "100%": { transform: "translateZ(0) rotate(8deg)" },
});

export const memoAnimation = keyframes({
  "0%": { transform: "translateZ(0) rotate(-7deg)" },
  "50%": { transform: "translateZ(0) rotate(-1deg)" },
  "100%": { transform: "translateZ(0) rotate(-7deg)" },
});

export const effect6_1Animation = keyframes({
  "0%": { transform: "translateZ(0) translateY(0)" },
  "50%": { transform: "translateZ(0) translateY(-5px)" },
  "100%": { transform: "translateZ(0) translateY(0)" },
});

export const effect6_2Animation = keyframes({
  "0%": { transform: "translateZ(0) translateY(0)" },
  "50%": { transform: "translateZ(0) translateY(-3px)" },
  "100%": { transform: "translateZ(0) translateY(0)" },
});

export const effect6_3Animation = keyframes({
  "0%": { transform: "translateZ(0) translateY(0)" },
  "50%": { transform: "translateZ(0) translateY(-2.98px)" },
  "100%": { transform: "translateZ(0) translateY(0)" },
});

export const effect6_4Animation = keyframes({
  "0%": { transform: "translateZ(0) translateY(0)" },
  "50%": { transform: "translateZ(0) translateY(-6px)" },
  "100%": { transform: "translateZ(0) translateY(0)" },
});

export const penAnimation = keyframes({
  "0%": { transform: "translateZ(0) translateY(0)" },
  "50%": { transform: "translateZ(0) translate(15px, -13px)" },
  "100%": { transform: "translateZ(0) translateY(0)" },
});

export const textBalloon8Animation = keyframes({
  "0%": { transform: "translateZ(0) translateY(0)" },
  "50%": { transform: "translateZ(0) translateY(-12px)" },
  "100%": { transform: "translateZ(0) translateY(0)" },
});

export const textBalloon9Animation = keyframes({
  "0%": { transform: "translateZ(0) translateY(0)" },
  "50%": { transform: "translateZ(0) translateY(-10px)" },
  "100%": { transform: "translateZ(0) translateY(0)" },
});

export const effect10Animation = keyframes({
  "0%": { transform: "translateZ(0) translateY(0)" },
  "50%": { transform: "translateZ(0) translate(-8px, -11px)" },
  "100%": { transform: "translateZ(0) translateY(0)" },
});

export const phoneAnimation = keyframes({
  "0%": { transform: "translateZ(0) rotate(6deg)" },
  "50%": { transform: "translateZ(0) rotate(-6deg)" },
  "100%": { transform: "translateZ(0) rotate(6deg)" },
});

export const phoneEffectAnimation = keyframes({
  "0%": { transform: "translateZ(0) translate(0, 0)" },
  "50%": { transform: "translateZ(0) translate(8px, -10px)" },
  "100%": { transform: "translateZ(0) translate(0, 0)" },
});

export const textBalloonAnimation12 = keyframes({
  "0%": { transform: "translateZ(0) translateY(0)" },
  "50%": { transform: "translateZ(0) translateY(-10px)" },
  "100%": { transform: "translateZ(0) translateY(0)" },
});
